using UnityEngine;
using System.Collections;

public class PelletScript : MonoBehaviour {

	//the class is only used to store a unique ID for each pellet
	public int id = -1;

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
